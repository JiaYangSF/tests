package sdsu.ds.tree;

import test.jia.Runner;

public class BinaryTree extends Runner {
	class Node {
		Node left, right;
		int data;

		Node(int data) {
			this.data = data;
			left = right = null;
		}
	}

	/*
	 * Add element: if less than root -- left. 
	 * if more than root -- right
	 */
	
}
